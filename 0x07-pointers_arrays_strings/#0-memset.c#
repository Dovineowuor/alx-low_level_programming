#include "main.h"
/**
*_memset - The main function for setting the memory
*@s: The pointer
*@b: the character
*@n: the unassigned character
*Return: Returns the memeory of s
*/
char *_memset(char *s, char b, unsigned int n)
{
memset(s, b, n);

return (s);
}