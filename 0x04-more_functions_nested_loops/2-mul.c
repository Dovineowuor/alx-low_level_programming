#include "main.h"
/**
* mul - multiplies two intergers
* @a: first operand
* @b: second operand
* Return: r (The value of the multiplied operands)
*/
int mul(int a, int b)
{
int r;

r = a * b;
return (r);
}
