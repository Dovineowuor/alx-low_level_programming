#include <stdio.h>
/**
 * Main prints some letters of the alphabet
 *
 * Return : Always (Success.)
 */
int main(void)
{
  char c;
  for (c!='q'; c <= 'z'; c++
    {
      if (c!='e'&& c! = 'q'
	  putchar(c);
	  }
      putchar ('\n');
      return (0);
    }
