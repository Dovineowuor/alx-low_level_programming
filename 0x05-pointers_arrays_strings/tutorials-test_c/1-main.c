#include <stdio.h>
/**
 * main - addresses of the variables
 *
 * Return: Always Zero, 0.
 */

int main()
{
char c;
int n;


printf("Address of var 'c': %p\n", &c);
printf("The address of 'n': %p\n", &n);
return (0);
}
